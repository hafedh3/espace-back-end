{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <pre>
                    {{ dump(personne) }}
                </pre>

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{ personne.firstname }} {{ personne.lastname }}</h5>
                        <p class="card-text text-success">Le personne est ajouté avec succès...</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
